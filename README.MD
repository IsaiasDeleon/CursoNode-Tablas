# Notas:
Este es mi primer porgrama en node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -n, --num      Es el numero que va agenrar la tabla        [number] [required]
  -l, --listar   indica si se emprime en consola o no [boolean] [default: false]
  -h, --hasta    el numero que se ingrese sera hasta donde se generera la tabla
                                                          [number] [default: 10]
```